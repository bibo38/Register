name: Register
version: 0.82
main: me.bibo38.Register.Register
author: bibo38
softdepend: [Vault]
commands:
  register:
    description: Register you on the Server
    usage: /register [Password]
permissions:
  register.register:
    description: Allows a player to use the register command!
    default: false